// TODO: Create a skeleton loader that matches the page layout.
const Loading = () => {
  return (
    <div style={{
      width: '100%',
      minHeight: '100vh',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    }}>
      <p>Loading...</p>
    </div>
  );
};

export default Loading;
